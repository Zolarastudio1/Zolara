name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build project
      run: npm run build
      env:
        VITE_SUPABASE_URL: https://tlepcrmwidzbkmkvmpdd.supabase.co
        VITE_SUPABASE_PUBLISHABLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRsZXBjcm13aWR6Ymtta3ZtcGRkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzMwMDU2NTcsImV4cCI6MjA4ODU4MTY1N30.7C1zVrszok4LWjB3mgRnSoftdq3UnSykQO41BcwXons

    - name: Deploy successful
      run: echo "✅ Build passed. Vercel is deploying automatically."
